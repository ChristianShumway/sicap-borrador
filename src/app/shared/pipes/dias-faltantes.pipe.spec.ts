import { DiasFaltantesPipe } from './dias-faltantes.pipe';

describe('DiasFaltantesPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasFaltantesPipe();
    expect(pipe).toBeTruthy();
  });
});
