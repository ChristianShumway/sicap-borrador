import { GetEmpresaPipe } from './get-empresa.pipe';

describe('GetEmpresaPipe', () => {
  it('create an instance', () => {
    const pipe = new GetEmpresaPipe();
    expect(pipe).toBeTruthy();
  });
});
