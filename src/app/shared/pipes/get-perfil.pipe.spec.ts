import { GetPerfilPipe } from './get-perfil.pipe';

describe('GetPerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
