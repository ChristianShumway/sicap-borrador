import { ToDateLongPipe } from './to-date-long.pipe';

describe('ToDateLongPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDateLongPipe();
    expect(pipe).toBeTruthy();
  });
});
